#!/bin/sh

case "$1" in
	start)
		echo "Starting simple server"
		start-stopdaemon -S -n simpleserver /usr/bin/aesdsocket -- -d
		;;
	stop)
		echo "Stoping simple server"
		start-stopdaemon -K -n simpleserver
		;;
	*)
		echo "Usage: $0 {start|stop}"
     		exit 1
 esac
 
 exit 0
